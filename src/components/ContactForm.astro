---
import { defaultLang, type Lang } from '../i18n/ui';
import { useTranslations } from '../i18n/utils';

interface Props {
  lang?: Lang;
}

const { lang = defaultLang } = Astro.props;
const t = useTranslations(lang);
---

<form
  name="contact"
  method="POST"
  action={`/${lang}/thanks/`}
  data-netlify="true"
  class="flex flex-col gap-4"
>
  <input type="hidden" name="form-name" value="contact" />

  <div>
    <label for="cf-name" class="sr-only">{t('contact.field.name')}</label>
    <input
      id="cf-name"
      type="text"
      name="name"
      placeholder={t('contact.field.name')}
      required
      class="field w-full px-4 py-3 text-sm"
    />
  </div>

  <div>
    <label for="cf-email" class="sr-only">{t('contact.field.email')}</label>
    <input
      id="cf-email"
      type="email"
      name="email"
      placeholder={t('contact.field.email')}
      required
      class="field w-full px-4 py-3 text-sm"
    />
  </div>

  <div>
    <label for="cf-message" class="sr-only">{t('contact.field.message')}</label>
    <textarea
      id="cf-message"
      name="message"
      rows="4"
      placeholder={t('contact.field.message')}
      required
      class="field w-full px-4 py-3 text-sm resize-y"
    ></textarea>
  </div>

  <button type="submit" class="submit-btn self-start px-6 py-3 text-sm font-semibold">
    {t('contact.cta')}
  </button>
</form>

<style>
  .field {
    display: block;
    font-family: var(--font-display);
    background: var(--bg-base);
    border: var(--border-width) solid var(--border);
    border-radius: var(--radius);
    color: var(--text-primary);
  }

  .field::placeholder {
    color: var(--text-secondary);
  }

  .field:focus-visible {
    border-color: var(--accent-from);
  }

  .submit-btn {
    background: var(--accent-from);
    color: var(--on-accent);
    border: var(--border-width) solid var(--accent-from);
    border-radius: var(--radius);
    cursor: pointer;
    box-shadow: var(--shadow);
  }

  .submit-btn:hover,
  .submit-btn:focus-visible {
    opacity: 0.9;
    box-shadow: var(--shadow-hover);
  }
</style>
